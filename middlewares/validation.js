export const validateBody = (schema) => {
  return (req, res, next) => {
    next();
  };
};

export const validateParams = (schema) => {
  return (req, res, next) => {
    next();
  };
};

export const validateQuery = (schema) => {
  return (req, res, next) => {
    next();
  };
};

